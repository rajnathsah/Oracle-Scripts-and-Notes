show parameter;

v$parameter
 all parameter
v$parameter2
 displays value of spfile, if spfile not in use then it display value as false 
v$spparameter
  displays parameter value which are in use
v$system_parameter
  displays parameter that are currently in effect for the instance
  
  
create pfile from spfile
create pfile ='initORCL.ora' from spfile; 

shutdown database

rename spfile to some other file

startup and check spfile parameter to see the which file is used

to keep state of pluggable database open after database restart, open pluggable database and save its state

alter pluggable database orclpdb save state;

remove the saved state

alter pluggable database orclpdb discard state;